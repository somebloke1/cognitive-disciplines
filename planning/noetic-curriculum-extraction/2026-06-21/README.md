# Noetic Curriculum Extraction - 2026-06-21

This is an untracked review bundle copied from `/home/dgk/workspace/noetic-pi`
for processing into stronger cognitive-discipline priming and acceptance gates.

## Purpose

The current `cognitive-disciplines` skills prove sequencing and packet shape, but
they do not yet reproduce the behavior-reinforcing curriculum and harness
discipline from noetic-pi. This bundle preserves the relevant source material so
the next implementation pass can compress it into specialist priming modules and
artifact-first controller gates.

## Raw Material

- `raw/method-curricula/`: noetic-pi curriculum JSON.
- `raw/method-prompts/`: Phronesis, EP audit, coordination, and curriculum prompts.
- `raw/method-sources/`: longer theory/source documents.
- `raw/method-core/`: imperatives, constitution, emergent properties, model participation, and root prompt.
- `raw/apm-src/`: APM source files that enforce phronesis grounding, progression, lifecycle, curriculum, archive, prompts, and tool profiles.
- `raw/apm-tests/`: phronesis flow, curriculum, lifecycle, state-machine, and ideal-conformance tests.
- `raw/project-root/`: project context copied for review.

## Extraction Findings

Three extraction agents inspected the source project:

- Curriculum/prompt extractor: identified `differentiated-cognition.json` and the phronesis prompt set as the main material for compressed specialist priming.
- Theory/imperatives extractor: identified the core operational concepts to compress, especially attention before interpretation, structural possibility generation, skeptical sufficiency judgment, and authority-bound decision.
- Harness/test extractor: identified the crucial missing behavior gates: role acknowledgment, staged grounding, active/submitted legality, accumulated context retrieval, recursion constraints, terminal states, and state-machine tests.

## Processing Direction

Do not paste the raw material wholesale into every skill. That would produce
jargon and weak imitation. Instead:

1. Create compressed specialist primers for P1, P2, P3, P4, and same-phase integrators.
2. Add artifact-first grounding packets before phase work.
3. Add controller acceptance gates that reject shallow or premature packets.
4. Maintain ordered archives and require downstream agents to read accumulated packets.
5. Translate noetic-pi tool mechanics into Codex controller procedures rather than preserving noetic-pi tool names.

## Strongest Missing Gates

- Mandatory specialist priming before P1/P2/P3/P4 work.
- Required grounding artifact per stage.
- Acceptance test against `orienting_question`, `implicit_unknown`, and phase role.
- Rejection/repair path for shallow packets.
- Persistent ordered archive as downstream substrate.
- Hard recursion contract with target, reason, content, and downstream rerun policy.
- Legal transition matrix for controller actions.

## Status

This bundle is intentionally untracked. It is ready for review and processing
into a feature plan or implementation issue.
