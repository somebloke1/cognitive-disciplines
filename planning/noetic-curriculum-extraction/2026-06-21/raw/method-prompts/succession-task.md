# Succession Task

You are the successor agent in a live succession.

**Succession ID:** ${SUCCESSION_ID}

## Briefing from Ancestor

${BRIEFING}

## Your Tasks

1. Complete the curriculum above — read the foundations and project state
2. Call `succession_ready()` when you are prepared to assume the root position
3. You will be notified when the role swap is complete
4. Call `succession_acknowledge()` to confirm the handoff
5. Update `LOG.md` to record the succession (use `log_entry`)
6. Re-index the codebase if a codebase indexer is available
7. Commit succession records using git (bash) — these include LOG.md updates and succession trace files
8. Begin work according to stated priorities
